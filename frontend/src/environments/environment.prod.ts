// src/environments/environment.prod.ts
// Este archivo es el entorno de producción.
// La variable 'production' es true.

export const environment = {
  production: true,
  backendUrl: 'URL_DE_TU_BACKEND_EN_PRODUCCION', // ¡CAMBIA ESTO a la URL de tu backend en producción!
  // Aquí irían otras variables de entorno para producción
};
